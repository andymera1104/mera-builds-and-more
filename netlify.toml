[build]
  command = "npm run build"
  # Con NITRO_PRESET=netlify los archivos publicos quedan en dist/
  # y el servidor (formularios, chat, correos) en .netlify/functions-internal/
  publish = "dist"

[build.environment]
  NODE_VERSION = "20"
  NITRO_PRESET = "netlify"
